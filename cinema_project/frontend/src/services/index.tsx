// src/services/index.ts
const API_URL = 'http://localhost:3000';

export const endpoints = {
  filmes: `${API_URL}/filmes`,
  salas: `${API_URL}/salas`,
  sessoes: `${API_URL}/sessoes`,
  ingressos: `${API_URL}/ingressos`,
};